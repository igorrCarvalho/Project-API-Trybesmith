import express from 'express';
/* import { loginSucceed } from '../controllers/loginController'; */

const loginRouter = express.Router();

/* loginRouter.get('/', loginSucceed); */

export default loginRouter;